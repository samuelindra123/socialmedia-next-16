# 🎯 Visual Guide: Role System

## 🔄 Workflow Lengkap

```
╔════════════════════════════════════════════════════════════╗
║                    USER SIGNUP FLOW                        ║
╚════════════════════════════════════════════════════════════╝

    👤 User baru mengunjungi aplikasi
          │
          ├──→ Klik "Sign Up"
          │
          ▼
    ┌─────────────────────────────┐
    │   /sign-up Page             │
    │   ✝️ "Bergabunglah Bersama" │
    │   (Clerk SignUp Component)  │
    └──────────┬──────────────────┘
               │
               ├──→ User isi form (email, password)
               │
               ▼
    ┌─────────────────────────────┐
    │   Clerk Creates Account     │
    │   Metadata: {}  (kosong)    │
    └──────────┬──────────────────┘
               │
               ├──→ Account created ✅
               │
               ▼
    ┌─────────────────────────────┐
    │   User Auto Login           │
    └──────────┬──────────────────┘
               │
               ▼
    ┌─────────────────────────────┐
    │   App Code Checks Role:     │
    │   role || 'user'            │
    │   Result: "user" ✅         │
    └──────────┬──────────────────┘
               │
               ▼
    ┌─────────────────────────────┐
    │   Header Shows:             │
    │   👤 User                   │
    └─────────────────────────────┘


╔════════════════════════════════════════════════════════════╗
║                 PROMOTE TO ADMIN FLOW                      ║
╚════════════════════════════════════════════════════════════╝

    👑 Admin ingin promote user
          │
          ├──→ Login Clerk Dashboard
          │
          ▼
    ┌─────────────────────────────┐
    │   dashboard.clerk.com       │
    │   Pilih App: careful-asp-35 │
    └──────────┬──────────────────┘
               │
               ├──→ Klik "Users" di sidebar
               │
               ▼
    ┌─────────────────────────────┐
    │   User List                 │
    │   • user1@example.com       │
    │   • user2@example.com       │
    │   • user3@example.com       │
    └──────────┬──────────────────┘
               │
               ├──→ Click user yang mau dijadikan admin
               │
               ▼
    ┌─────────────────────────────┐
    │   User Detail Page          │
    │   Email: user1@example.com  │
    │   ...                       │
    │   Metadata Section          │
    └──────────┬──────────────────┘
               │
               ├──→ Scroll ke "Public metadata"
               ├──→ Click "Edit" ✏️
               │
               ▼
    ┌─────────────────────────────┐
    │   Edit Metadata             │
    │   ┌───────────────────────┐ │
    │   │ {                     │ │
    │   │   "role": "admin"     │ │ ← Type this
    │   │ }                     │ │
    │   └───────────────────────┘ │
    │   [Save]  [Cancel]          │
    └──────────┬──────────────────┘
               │
               ├──→ Click "Save"
               │
               ▼
    ┌─────────────────────────────┐
    │   Metadata Updated ✅       │
    │   Public: { "role":"admin" }│
    └──────────┬──────────────────┘
               │
               ▼
          User harus RE-LOGIN!
               │
               ▼
    ┌─────────────────────────────┐
    │   User Logout dari App      │
    └──────────┬──────────────────┘
               │
               ▼
    ┌─────────────────────────────┐
    │   User Login Kembali        │
    └──────────┬──────────────────┘
               │
               ▼
    ┌─────────────────────────────┐
    │   App Reads New Metadata    │
    │   Role: "admin" ✅          │
    └──────────┬──────────────────┘
               │
               ▼
    ┌─────────────────────────────┐
    │   Header Shows:             │
    │   👑 Admin                  │
    └──────────┬──────────────────┘
               │
               ▼
    Admin Features Unlocked! 🎉


╔════════════════════════════════════════════════════════════╗
║              ROLE-BASED ACCESS EXAMPLE                     ║
╚════════════════════════════════════════════════════════════╝

User mengakses: /dashboard
          │
          ▼
    App checks role...
          │
          ├─────────────────┬─────────────────┐
          │                 │                 │
    Role: "user"      Role: "admin"     Not logged in
          │                 │                 │
          ▼                 ▼                 ▼
    ┌──────────┐      ┌──────────┐      ┌──────────┐
    │ User     │      │ Admin    │      │ Redirect │
    │ Dashboard│      │ Dashboard│      │ /sign-in │
    │          │      │ +        │      └──────────┘
    │ • Posts  │      │ Controls │
    │ • Profile│      │          │
    │          │      │ • Delete │
    └──────────┘      │ • Manage │
                      │ • Stats  │
                      └──────────┘


╔════════════════════════════════════════════════════════════╗
║                  COMPONENT EXAMPLE                         ║
╚════════════════════════════════════════════════════════════╝

<Dashboard>
    │
    ├─ <AdminOnly>
    │    │
    │    └─→ Role: "admin" ✅  → Shows content
    │         Role: "user"  ❌  → Hidden
    │         Not logged in ❌  → Hidden
    │
    ├─ <UserOnly>
    │    │
    │    └─→ Role: "admin" ❌  → Hidden
    │         Role: "user"  ✅  → Shows content
    │         Not logged in ❌  → Hidden
    │
    └─ Public Content
         │
         └─→ Everyone ✅  → Shows content


╔════════════════════════════════════════════════════════════╗
║              CLERK DASHBOARD STRUCTURE                     ║
╚════════════════════════════════════════════════════════════╝

https://dashboard.clerk.com
    │
    ├─ Applications
    │   └─ careful-asp-35 ← Your app
    │       │
    │       ├─ Users ← You work here!
    │       │   │
    │       │   ├─ User List
    │       │   │   └─ Click user
    │       │   │       └─ Scroll to Metadata
    │       │   │           └─ Edit Public metadata
    │       │   │               └─ Set: { "role": "admin" }
    │       │   │
    │       │   └─ Metadata shows:
    │       │       • Empty {} = user (default)
    │       │       • { "role": "admin" } = admin
    │       │
    │       ├─ Sessions
    │       ├─ API Keys ← Your keys here
    │       └─ Settings


╔════════════════════════════════════════════════════════════╗
║                  SECURITY LAYERS                           ║
╚════════════════════════════════════════════════════════════╝

Request: GET /admin/dashboard
    │
    ├─→ Layer 1: Clerk Authentication
    │   └─ Logged in? → ✅ Continue | ❌ Redirect /sign-in
    │
    ├─→ Layer 2: ProtectedRoute Component
    │   └─ Has required role? → ✅ Continue | ❌ Redirect /
    │
    ├─→ Layer 3: Role Check in Component
    │   └─ role === 'admin'? → ✅ Show | ❌ Hide
    │
    └─→ Layer 4: API Route Protection
        └─ Validate role on server → ✅ Process | ❌ 403


╔════════════════════════════════════════════════════════════╗
║                   CODE FLOW                                ║
╚════════════════════════════════════════════════════════════╝

User Request
    │
    ▼
Middleware (src/middleware.ts)
    │ clerkMiddleware()
    │ → Verifies auth token
    │
    ▼
Layout (src/app/layout.tsx)
    │ <ClerkProvider>
    │ → Provides auth context
    │
    ▼
Page Component
    │ Checks: await getUserRole()
    │
    ├─→ Server Component:
    │   import { getUserRole } from '@/lib/utils/auth'
    │   const role = await getUserRole()
    │   └─→ auth() → sessionClaims → metadata.role || 'user'
    │
    └─→ Client Component:
        import { useUser } from '@clerk/nextjs'
        const role = user?.publicMetadata?.role || 'user'
        └─→ Clerk context → user object → metadata


╔════════════════════════════════════════════════════════════╗
║              ROLE STATES & VISIBILITY                      ║
╚════════════════════════════════════════════════════════════╝

Component: <ProtectedRoute requiredRole="admin">

┌─────────────────┬──────────────────┬──────────────────┐
│   Not Logged In │    Role: "user"  │  Role: "admin"   │
├─────────────────┼──────────────────┼──────────────────┤
│ Redirect to     │ Redirect to      │ ✅ Show Content  │
│ /sign-in        │ / (home)         │                  │
│                 │                  │                  │
│ ❌ Access Denied │ ❌ Access Denied  │ ✅ Full Access   │
└─────────────────┴──────────────────┴──────────────────┘


Component: <AdminOnly>{content}</AdminOnly>

┌─────────────────┬──────────────────┬──────────────────┐
│   Not Logged In │    Role: "user"  │  Role: "admin"   │
├─────────────────┼──────────────────┼──────────────────┤
│ ❌ Hidden        │ ❌ Hidden         │ ✅ Visible       │
│ (returns null)  │ (returns null)   │ (shows content)  │
└─────────────────┴──────────────────┴──────────────────┘


Component: <UserOnly>{content}</UserOnly>

┌─────────────────┬──────────────────┬──────────────────┐
│   Not Logged In │    Role: "user"  │  Role: "admin"   │
├─────────────────┼──────────────────┼──────────────────┤
│ ❌ Hidden        │ ✅ Visible        │ ❌ Hidden        │
│ (returns null)  │ (shows content)  │ (returns null)   │
└─────────────────┴──────────────────┴──────────────────┘


╔════════════════════════════════════════════════════════════╗
║                 QUICK CHEAT SHEET                          ║
╚════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────┐
│ CHECK ROLE (Server Component)                            │
├──────────────────────────────────────────────────────────┤
│ import { getUserRole, isAdmin } from '@/lib/utils/auth'  │
│                                                          │
│ const role = await getUserRole()  // 'admin' | 'user'   │
│ const admin = await isAdmin()     // true | false       │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│ CHECK ROLE (Client Component)                            │
├──────────────────────────────────────────────────────────┤
│ 'use client'                                             │
│ import { useUser } from '@clerk/nextjs'                  │
│                                                          │
│ const { user } = useUser()                               │
│ const role = user?.publicMetadata?.role || 'user'        │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│ PROTECT PAGE                                             │
├──────────────────────────────────────────────────────────┤
│ import { ProtectedRoute } from '@/components/auth'       │
│                                                          │
│ <ProtectedRoute requiredRole="admin">                   │
│   <YourComponent />                                      │
│ </ProtectedRoute>                                        │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│ CONDITIONAL RENDER                                       │
├──────────────────────────────────────────────────────────┤
│ import { AdminOnly, UserOnly } from '@/components/auth'  │
│                                                          │
│ <AdminOnly>                                              │
│   <AdminContent />                                       │
│ </AdminOnly>                                             │
│                                                          │
│ <UserOnly>                                               │
│   <UserContent />                                        │
│ </UserOnly>                                              │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│ PROTECT API ROUTE                                        │
├──────────────────────────────────────────────────────────┤
│ import { auth } from '@clerk/nextjs/server'              │
│ import { getUserRole } from '@/lib/utils/auth'           │
│                                                          │
│ export async function POST() {                           │
│   const { userId } = await auth()                        │
│   if (!userId) return Response.json({}, {status: 401})  │
│                                                          │
│   const role = await getUserRole()                       │
│   if (role !== 'admin') {                                │
│     return Response.json({}, {status: 403})             │
│   }                                                      │
│   // Admin logic...                                      │
│ }                                                        │
└──────────────────────────────────────────────────────────┘
```

---

**🎯 Summary**: Simple & powerful role system tanpa ribet setup!
